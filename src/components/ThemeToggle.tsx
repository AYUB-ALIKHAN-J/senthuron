
import * as React from "react";

// Removing the theme toggle functionality as we're standardizing on light theme
const ThemeToggle: React.FC = () => {
  // This component is now empty since we don't need a theme toggle
  return null;
};

export default ThemeToggle;
